# Combines a list into a set

# Set and list of colors
list_colors = ['yellow','orange','black']
set_colors = {'blue', 'green', 'red'}

# Combining list into set of colors
print(set_colors.union(list_colors))
